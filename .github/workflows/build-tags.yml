name: Build and Upload Penpot TAGS Bundles

on:
  push:
    tags:
      - '*'

jobs:
  build-tag-bundle:
    uses: ./.github/workflows/build-bundles.yml
    secrets: inherit
    with:
      gh_ref: ${{ github.ref_name }}
